server:
  port:

spring:
  cloud:
    config:
      server:
        git:
          uri:
          clone-on-start:
          ignore-local-ssh-settings:
          default-label:
          host-key-algorithm:
          strict-host-key-checking:
          host-key:
          privateKey:
